#include <stdio.h>

/**
*main -> Prints the last digit of a randomly generated
*dgsdfsds
*Return: Always 0
*/
int main(void)
{
int ch;
for (ch = 97; ch <= 122; ch++)
{
putchar(ch);
}
putchar(10);

return (0);
}
